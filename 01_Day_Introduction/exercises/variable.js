//Declaration of Variables

let name = "Reister Ruedas";
let boolean = false;
let undf = undefined;
let empty = null;

//Declare 4 variables without assigning values
let firstVariable, secondVariable, thirdVariable, fourthVariable;

// Declare 4 variables with assigned values
let _firstVar = 1, _secondVar = "second", _thirdVar = true; _fourthVar = undefined;

// Declare variables with name, status, country, and age in multiple lines
let _fname = 'Reister';
let _lname = 'Ruedas';
let _status = 'Single';
let _country = 'PH';
let _age = 23;

// Declare variables with name, status, country and age in single line
let fname = 'Reister', lname = 'Ruedas', status = 'Single', country = 'PH', age = 23;

let myAge = 23, yourAge = 22;

console.log(myAge," ",yourAge);